services:
  - type: static
    name: lbap-corporate
    env: static
    buildCommand: npm install && npm run build
    staticPublishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
