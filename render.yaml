services:
  - type: web
    name: lbap-corporate
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
